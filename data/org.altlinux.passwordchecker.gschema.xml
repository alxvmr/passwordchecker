<?xml version="1.0" encoding="utf-8"?>

<schemalist>

  <schema path="/org/altlinux/passwordchecker/" id="org.altlinux.passwordchecker">

    <key name='url' type='s'>
      <default>""</default>
      <summary>LDAP server address</summary>
      <description>
        Specifies the LDAP server address (e.g. "ldap://dc1.domain.test.ru")
      </description>
    </key>

    <key name='base-dn' type='s'>
      <default>""</default>
      <summary>Search root</summary>
      <description>
        Specifies the search root for the desired record (e.g. "dc=domain,dc=test,dc=ru")
      </description>
    </key>

    <key name='start-warning-time' type='x'>
      <default>17520</default>
      <summary>How long to warn the user about password expiration</summary>
      <description>
        A password expiration warning will be displayed for the specified time. The time is specified in hours
      </description>
    </key>

    <key name='warning-frequencies' type='x'>
      <default>4</default>
      <summary>Frequency of warning output</summary>
      <description>
        Sets the frequency of password change warning output. The parameter is set in hours
      </description>
    </key>

  </schema>

</schemalist>